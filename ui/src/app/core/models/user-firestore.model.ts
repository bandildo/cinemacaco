export interface UserFirestore {
    fields: {
      uid: {
        stringValue: string;
      };
      email: {
        stringValue: string;
      };
      admin: {
        booleanValue: boolean;
      };
      macaco: {
        booleanValue: boolean;
      };
    };
  }